# Character Creator

Open `character-sheet.html` to use the character creator.

- `feats.json` is the editable feat database.
- `mantles.json` is the editable mantle database.
- `pictures/` is for keeping source character images.
